import 'package:flutter/material.dart';

class SelRotiPage extends StatelessWidget {
  const SelRotiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    body: Container(height: 400,width: 300,
    child: Image.asset('assets/images/selroti.png'),),
    );
  }
}
